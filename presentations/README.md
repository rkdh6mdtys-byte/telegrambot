# Presentations

Place the presentation file here as `presentation.pdf`.

The bot will read and send `presentations/presentation.pdf` when a user taps the **🖼️ Презентация** button in the main menu.

If the file is missing, the bot will display a graceful "temporarily unavailable" message instead of crashing.
